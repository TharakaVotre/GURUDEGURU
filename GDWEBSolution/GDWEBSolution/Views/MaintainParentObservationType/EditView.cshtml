@model GDWEBSolution.Models.Maintenance.ParentObservationTypeModel


<div>
   
    <form id="myForm" class="form-horizontal">
        <div class="modal-header">
            <button type="button" class="close" data-dismiss="modal">&times;</button>
            <h4 class="modal-title">Edit Message Type</h4>
        </div>
        <div class="modal-body">
           
                <div style="display:none">
                    @Html.TextBoxFor(model => model.ObTypeId)
                   
                </div>

            

            <div class="form-group">

               
                @Html.LabelFor(m => m.Description, new { @class = "col-sm-3 control-label" })
                <div class="col-sm-8">
                    @Html.TextBoxFor(model => model.Description, new { @class = "form-control", @placeholder = "Parent Observation Type" })
                    <span class="help-block m-b-none" id="NameError"></span>
                </div>
            </div>

        

               </div>
        <div class="modal-footer">

             <a href="#" id="btnSubmit" class="btn btn-primary">Save</a>
        </div>
    </form>


</div>
<script>

    $(document).ready(function () {

        $("#btnSubmit").click(function (e) {
            debugger
           

            var Name = $('#Description');
            var NameError = $('#NameError');


          
            // Check if there is an entered value
            if (!Name.val()) {
                // Add errors highlight
                CategoryName.closest('.form-group').removeClass('has-success').addClass('has-error');
                $("#NameError").text("Required");
                // Stop submission of the form
               // e.preventDefault();
                return;
            }
            // Check if there is an entered value
          
           

            var myformdata = $("#myForm").serialize();
          
            $.ajax({

                type: "POST",
                url: "/MaintainParentObservationType/Edit",
                data: myformdata,
                success: function () {
                    //$("#loaderDiv").hide();
                    $("#myModal").modal("hide");
                    window.location.href = "/MaintainParentObservationType/Index";

                }

            })
        })

    })


</script>

