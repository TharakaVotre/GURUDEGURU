
@model GDWEBSolution.Models.Schools.SchoolGradeModel


<div>
    <form id="FormQDelete" class="form-horizontal">
        <div class="modal-header">
            <button type="button" class="close" data-dismiss="modal">&times;</button>
            <h5 class="modal-title">Are you sure you want to Delete this Grade ? @Html.HiddenFor(m => m.SchoolId)</h5>
            @Html.HiddenFor(m => m.GradeId)
          
        </div>
        <div class="modal-footer">

            <a href="#" id="btnQDSubmit" class="btn btn-primary">YES</a>
            <a href="#" id="btnNo" class="btn btn-default">NO</a>
        </div>
    </form>
    @*    <div style="text-align: center; display: none" id="loaderDiv">
            Loading
        </div>*@
</div>
<script>

    $(document).ready(function () {

        $("#btnQDSubmit").click(function (e) {
            debugger
            var myformdata = $("#FormQDelete").serialize();
            $.ajax({

                type: "POST",
                url: "/School/DeleteSchoolgrades",
                data: myformdata,
                success: function (result) {
                    //$("#loaderDiv").hide();
                    if (result != "Error") {
                        debugger
                        //window.location.href = "/Teacher/Create";
                        // $("#TeacherID").val(result);
                        //  $('#ResultDIV').load('/Teacher/ShowTeacherQualificatoin', ShowTeacherQualificatoinAction);
                        $('#showgrade').load('/School/ShowSchoolGrade?SchoolId=' + result);
                    }
                    $("#deleteModel").modal("hide");


                }

            })
        })

        $("#btnNo").click(function (e) {
            debugger
            $("#deleteModel").modal("hide");
        })

    })


</script>
