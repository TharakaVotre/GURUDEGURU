@model IEnumerable<GDWEBSolution.Models.AnnualFunctions.StudentGradeAdvanceModel>

@{
    ViewBag.Title = "Detail";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

@using (Html.BeginForm("Update", "StudentGradeAdvance", FormMethod.Post))
{


    <div class="form-horizontal">
        <div class="modal-header">
            <h4 class="modal-title">update student grade</h4>
        </div>

        <div class="panel panel-default">
            <div class="panel-heading">
                <div class="form-group">
                    <div class="row"> 
                        <lable class="col-sm-3 control-label">Selected Year:-@ViewBag.CurentYear</lable>
                        
                        <lable class="col-sm-3 control-label">Selected Grade:-@ViewBag.CurentGrade</lable>
                       
                        <lable class="col-sm-3 control-label">Selected Class:-@ViewBag.CurentClass</lable>
                       
                </div>
                    </div>
                <hr />
                <div class="form-group">
                    <div class="row">
                        <lable class="col-sm-2 control-label">Advance to Year</lable>
                        <div class="col-sm-3">

                            @Html.TextBox("AcedamicYear", "")

                        </div>
                        <lable class="col-sm-2 control-label">Advance to Grade</lable>
                        <div class="col-sm-3">
                            @Html.DropDownList("GradeId", ViewBag.country as List<SelectListItem>, "", new { @class = "form-control" })
                        </div>
                        </div>
                    </div>
                        <div class="form-group">
                            <div class="row">
                                <lable class="col-sm-2 control-label">Advance to Class</lable>
                                <div class="col-sm-3">
                                    @Html.DropDownList("ClassId", new SelectList("", "Value", "Text"), "", new { @class = "form-control" })
                                </div>
                                <div>
                                    <button type="submit" class="btn btn-primary"><span class="fa fa-flag"></span> Save</button>
                                </div>
                            </div>
                        </div>
                        <a href="~/StudentGradeAdvance/Index" class="btn btn-primary"><span class="fa fa-plus"></span>Go Back</a>

                    </div>
            <div class="panel-body">
                <table id="zctb" class="display table table-bordered table-hover" cellspacing="0" width="100%">
                    <thead>
                        <tr>
                            <th>
                                Select
                            </th>
                            <th>
                                @Html.DisplayNameFor(model => model.StudentId)
                            </th>
                            <th>
                                @Html.DisplayNameFor(model => model.StudentName)
                            </th>
                            <th>
                                @Html.DisplayNameFor(model => model.ClassName)
                            </th>
                            <th>
                                @Html.DisplayNameFor(model => model.HouseName)
                            </th>

                        </tr>
                    </thead>
                    <tbody>
                        @if (Model != null)
                        {
                            foreach (var item in Model)
                            {
                                <tr>
                                    <td>
                                        <input type="checkbox" name="selectedNames" value="@item.StudentId" checked /> <!-- HOW SHOULD THIS BE RENDERED? -->

                                    </td>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.StudentId)
                                    </td>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.StudentName)
                                    </td>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.ClassName)
                                    </td>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.HouseName)
                                    </td>

                                </tr>
                            }

                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
}
<style>
    #AcedamicYear {
        border: 1px solid #e0e0e0;
        padding: 5px 8px;
        color: #616161;
        background: #fff;
        box-shadow: none !important;
        display: block;
        width: 100%;
        height: 32px;
        font-size: 14px;
        line-height: 1.45;
        border-radius: 2px;
        transition: border-color ease-in-out .15s, box-shadow ease-in-out .15s;
    }
    
</style>


<script src="~/Scripts/jquery-1.10.2.min.js"></script>
<script type="text/javascript">
    $(document).ready(function () {
        $("#GradeId").change(function () {
            $("#ClassId").empty();
            $.ajax({
                type: 'POST',
                url: '@Url.Action("getstate")',
                dataType: 'json',
                data: { id: $("#GradeId").val() },
                success: function (states) {
                    $.each(states, function (i, state) {
                        $("#ClassId").append('<option value="' + state.Value + '">' +
                             state.Text + '</option>');
                    });
                },
                error: function (ex) {
                    alert('Failed to retrieve states.' + ex);
                }
            });
            return false;
        })
    });

</script>